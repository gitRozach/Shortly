import styled from 'styled-components';

const StyledHistoryBar = styled.div`
    display: flex;
    flex-direction: column;
    
    align-items: center;
    justify-content: stretch;
    
    position: relative;

    background: rgba(163, 150, 255, 0.2);
    font-family: 'Poppins', sans-serif;
    margin: 0;
    padding: 7rem 4rem 0rem 4rem;
`

export default StyledHistoryBar;